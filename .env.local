# Локальная разработка
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=secret
DB_NAME=miniapp

# Для SQLAlchemy с правильной кодировкой
SQLALCHEMY_DATABASE_URI=postgresql://postgres:secret@localhost:5432/miniapp?client_encoding=utf8
DATABASE_URL=postgresql://postgres:secret@localhost:5432/miniapp?client_encoding=utf8

# Redis для локальной разработки
REDIS_URL=redis://localhost:6379/0

# Flask настройки
FLASK_ENV=development
FLASK_DEBUG=True

# Socket.IO
SOCKETIO_REDIS_URL=redis://localhost:6379/0

# React для локальной разработки
REACT_APP_API_URL=http://localhost:4000
REACT_APP_SOCKET_URL=http://localhost:4000

# Email (можно оставить пустым для локальной разработки)
SMTP_SERVER=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
FROM_EMAIL=noreply@rgszh-miniapp.org